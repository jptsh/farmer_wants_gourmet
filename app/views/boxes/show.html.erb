
<div>
<h1><%= @box.name %></h1>
</div>

<p>Name:<%= @box.name %></p>
<p>Price in €: <%= @box.price %></p>
<p>Farmer's ID: <%= @box.user.first_name %>, <%= @box.user.last_name %></p>


<% @box.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>

<%= link_to 'Back to all boxes', boxes_path, class: 'btn btn-primary' %>

<%= simple_form_for ([@box, @order]) do |f| %>
<%= f.submit "Buy the box", class: 'btn btn-primary m-2 col-2' %>
<% end %>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @marker.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>

</div>